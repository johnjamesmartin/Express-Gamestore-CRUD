extends layout

block content
  h1=title
  hr
  h2=`${genre_detail.name} `
    span
      a(href=`/catalog/genre/${genre_detail.id}/delete`)
        button(class="btn btn-sm btn-danger")
          i(class="fa fa-trash-alt")
  p="Description: Not available"
  hr
  small=`Genre ID: ${genre_detail.id}`