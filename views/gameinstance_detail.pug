extends layout

block content
  h1= title
  hr
  h2=`${gameinstance_detail.game.title} (£${gameinstance_detail.price}) `
    span
      a(href=`/catalog/gameinstance/${gameinstance_detail.id}/delete`)
        button(class="btn btn-sm btn-danger")
          i(class="fa fa-trash-alt")
  h6=`${gameinstance_detail.console.toUpperCase()}`
  br
  p=`Description:`
    ul(class="list-group list-group-flush")
      each descriptor in gameinstance_detail.description.split("---")
        li(class="list-group-item")=descriptor
  br
  small=`Manufacturer: ${gameinstance_detail.game.developer}`
  br
  small=`Medium: ${gameinstance_detail.medium}`
  br
  small=`Release year: ${gameinstance_detail.game.releaseYear}`
  br
  small=`Product ID: ${gameinstance_detail.id}`