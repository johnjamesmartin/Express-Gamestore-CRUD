extends layout

block content
  h1= title
  p Welcome to the Gamestore. Here we sell second-hand videogames.

  h1 Inventory

  if error
    p Error getting dynamic content.
  else
    p Our store has record counts as follows:

    ul
      li #[strong Games:] !{data.game_count}
      li #[strong Copies:] !{data.game_instance_count}
      //ßli #[strong Copies available:] !{data.game_instance_available_count} 
      li #[strong Platforms:] !{data.platform_count}
      li #[strong Genres:] !{data.genre_count}